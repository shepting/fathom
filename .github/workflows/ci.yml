name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer

      - name: Show Xcode version
        run: xcodebuild -version

      - name: List available simulators
        run: xcrun simctl list devices available

      - name: Install xcbeautify
        run: brew install xcbeautify

      - name: Build and Test
        run: |
          set -o pipefail
          xcodebuild \
            -project Fathom.xcodeproj \
            -scheme Fathom \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.4' \
            clean test 2>&1 | tee xcodebuild.log | xcbeautify

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-and-test-log
          path: xcodebuild.log

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: ~/Library/Developer/Xcode/DerivedData/**/Logs/Test/*.xcresult

  screenshot:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer

      - name: Install xcbeautify
        run: brew install xcbeautify

      - name: Build and run app
        run: ./run.sh 2>&1 | tee run.log

      - name: Take screenshot
        run: ./screenshot.sh

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshot-build-log
          path: run.log

      - name: Upload screenshot
        uses: actions/upload-artifact@v4
        with:
          name: simulator-screenshot
          path: /tmp/simulator-screenshot.png

  ui-tests:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer

      - name: Boot simulator
        run: |
          xcrun simctl boot "iPhone 16" || true

      - name: Run UI Tests
        run: |
          xcodebuild \
            -project Fathom.xcodeproj \
            -scheme Fathom \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.4' \
            -only-testing:FathomUITests \
            test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ui-test-results
          path: |
            ~/Library/Developer/Xcode/DerivedData/**/Logs/Test/*.xcresult
