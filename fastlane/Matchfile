git_url("https://github.com/shepting/certificates")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["com.hepting.Fathom"])

username(ENV["PERSONAL_APPLE_ID"])

# For CI/CD - use API key instead of password when available
# api_key_path("fastlane/api_key.json")
